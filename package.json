{
  "name": "showmethesaju",
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "validate": "node --experimental-strip-types scripts/validate.mjs",
    "smoke": "node scripts/smoke-dist.mjs",
    "check": "npm run validate && npm run build && npm run smoke"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.1.6",
    "astro": "^4.16.0",
    "korean-lunar-calendar": "^0.3.6"
  }
}
